---
description: 
globs: 
alwaysApply: false
---
# AutoRegisterL 项目概述

这是一个Android项目，名为AutoRegisterL。项目的主要目的是实现自动注册功能。

## 项目结构

当开发进行时，以下是典型的Android项目文件结构：

- `app/` - 应用主模块
  - `src/main/` - 主要源代码
    - `java/` - Java/Kotlin源代码
    - `res/` - 资源文件
    - `AndroidManifest.xml` - 应用清单文件
- `gradle/` - Gradle包装器文件
- `build.gradle` - 项目级构建脚本
- `app/build.gradle` - 应用模块构建脚本
- `settings.gradle` - 项目设置文件

## 开发指南

在开发过程中，请遵循以下规范：

1. 使用标准的Android开发实践
2. 遵循Material Design设计规范
3. 合理组织代码结构，保持代码清晰可维护
4. 对核心功能进行单元测试

随着项目的发展，此规则文件将更新以反映当前的项目状态和开发指南。

